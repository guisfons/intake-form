const $win = $(window);
const $doc = $(document);
const $body = $("body");
const $htmlBody = $("html, body");
const SM_MOBILE_WIDTH = 575;
const MOBILE_WIDTH = 768;
const TABLET_WIDTH = 1024;

export {
  $win,
  $doc,
  $body,
  $htmlBody,
  SM_MOBILE_WIDTH,
  MOBILE_WIDTH,
  TABLET_WIDTH,
};
